<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Add Table</title>
  
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  {{> sidebar}}
  <div class="container  mb-3 col col-lg-6 border  border-effect" >
     <div  class="bg-secondary p-2 text-white ">
       <h5>Add Table</h5>
    </div>
  <div class="container mt-2 mb-2">
   
    <form class="" action="/tables" method="POST" >
      <div class="mb-2">
        <label for="tableName" class="form-label">Table Name</label>
        <input type="text" name="tableName" class="form-control" required>
      </div>
       <div class="mb-2">
        <label for="capacity" class="form-label">Capacity</label>
        <input type="number" name="capacity" class="form-control" required>
      </div>
      <div class="mb-2">
    <label for="tablestatusId" class="form-label">Table Status</label>
    <select name="tablestatusId" class="form-control" required>
       {{#each tablestatus}}
              <option value="{{this._id}}">{{this.statusName}}</option>
       {{/each}}
    </select>
</div>

   <div class="mb-2">
          <label for="storeId" class="form-label">Store Id</label>
          <select name="storeId" class="form-control" required>
            {{#each stores}}
              <option value="{{this._id}}">{{this.storeName}}</option>
            {{/each}}
          </select>
        </div>
    <br>
      <button type="submit" class="btn btn-success">Save</button>
        <a href="/tables" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
  </div>
</body>
</html>












