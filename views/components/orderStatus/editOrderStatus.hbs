<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Edit Order Status</title>
  
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  {{> sidebar}}
  <div class="container  mb-3 col col-lg-6 border  border-effect" >
  <div class="container mt-2 mb-2">
     <div class="bg-secondary p-2 text-white ">
       <h5>Edit Status</h5>
    </div>
    <form action="/orderstatus/{{orderstatus._id}}?_method=PUT" method="POST">
      <div class="mb-2">
        <label for="statusName" class="form-label">Status Name</label>
        <input type="text" name="statusName" class="form-control" value="{{orderstatus.statusName}}" required>
      </div>

 <div class="mb-2">
  <label for="storeId" class="form-label">Store</label>
  <select name="storeId" class="form-control" required>
      {{#each stores}}
      <option value="{{this._id}}" {{#if (eq this._id ../orderstatus.storeId)}}selected{{/if}}>
        {{this.storeName}}
      </option>
    {{/each}}
  </select>
</div>
    <br>
      <button type="submit" class="btn btn-primary">Update Status</button>
      <a href="/orderstatus" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
   </div>
</body>
</html>
