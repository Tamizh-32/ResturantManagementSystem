<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Edit Category</title>

  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    {{> sidebar}} 


  <div class="container  mb-3 col col-lg-6 border  border-effect" >
  <div class="container mt-2 mb-2">
     <div class="bg-secondary p-2 text-white ">
       <h5>Edit Category</h5>
    </div>
    <form action="/category/{{category._id}}?_method=PUT" method="POST">
      <div class="mb-2">
        <label for="categoryName" class="form-label">Category Name</label>
        <input type="text" name="categoryName" class="form-control" value="{{category.categoryName}}" required>
      </div>
   <div class="mb-2">
  <label for="status" class="form-label">Status</label>
  <select name="status" class="form-control" required>
    <option value="active" {{#if (eq category.status "active")}}selected{{/if}}>Active</option>
    <option value="inactive" {{#if (eq category.status "inactive")}}selected{{/if}}>Inactive</option>
  </select>
</div>
      
   <br>
      <button type="submit" class="btn btn-primary">Update</button>
      <a href="/category" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
   </div>
</body>
</html>
