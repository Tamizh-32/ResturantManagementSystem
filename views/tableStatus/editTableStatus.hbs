<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Edit Table Status</title>
</head>
<body>
  <div class="container mt-5 mb-3 col col-lg-6 border border-primary" >
  <div class="container mt-2 mb-2">
     <div class="bg-secondary p-2 text-white ">
       <h5>Edit Status</h5>
    </div>
    <form action="/tablestatus/{{tablestatus._id}}?_method=PUT" method="POST">
      <div class="mb-2">
        <label for="statusName" class="form-label">Status Name</label>
        <input type="text" name="statusName" class="form-control" value="{{tablestatus.statusName}}" required>
      </div>

 <div class="mb-2">
  <label for="storeId" class="form-label">Store</label>
  <select name="storeId" class="form-control" required>
      {{#each stores}}
      <option value="{{this._id}}" {{#if (eq this._id ../tablestatus.storeId)}}selected{{/if}}>
        {{this.storeName}}
      </option>
    {{/each}}
  </select>
</div>

      <button type="submit" class="btn btn-primary">Update Status</button>
      <a href="/tablestatus" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
   </div>
</body>
</html>
