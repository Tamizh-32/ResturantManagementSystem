<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Table Status List</title>
</head>
<body>
  
  <div class="container mt-5">
   
    <div class="d-flex bg-secondary text-white p-2 justify-content-between mb-3">
      <h5>Table Status List</h5>
      <div class=" me-2">
      <a href="/tablestatus/add" class="btn btn-primary">Add Status</a>
      </div>
    </div>
    <table class="table  table-bordered">
      <thead>
        <tr>
          <th>S.no</th>
          <th>Status Name</th>
          <th>Store Name</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {{#each tableStatus}}
        <tr>
          <td>{{inc @index}}</td>
          <td>{{this.statusName}}</td>
         <td>{{this.storeId.storeName}}</td>
          <td>
            <a href="/tablestatus/edit/{{this._id}}" class="btn btn-warning btn-sm">Edit</a>
            <form action="/tablestatus/{{this._id}}?_method=DELETE" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger btn-sm">Delete</button>
            </form>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</body>
</html>




