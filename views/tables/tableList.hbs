<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Table List</title>
</head>
<body>
  
  <div class="container mt-5">
   
    <div class="d-flex bg-secondary text-white p-2 justify-content-between mb-3">
      <h5>Tables List</h5>
      <div class=" me-2">
      <a href="/tables/add" class="btn btn-primary">Add Table</a>
      </div>
    </div>
    <table class="table  table-bordered">
      <thead>
        <tr>
          <th>S.no</th>
          <th>Table No</th>
          <th>Capacity</th>
          <th>Status</th>
          <th>Store Name</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {{#each tables}}
        <tr>
          <td>{{inc @index}}</td>
          <td>{{this.tableName}}</td>
          <td>{{this.capacity}}</td>
          <td>{{this.tablestatusId.statusName}}</td>
         <td>{{this.storeId.storeName}}</td>
          <td>
            <a href="/tables/edit/{{this._id}}" class="btn btn-warning btn-sm">Edit</a>
            <form action="/tables/{{this._id}}?_method=DELETE" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger btn-sm">Delete</button>
            </form>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</body>
</html>




