<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Stores List</title>
</head>
<body>
  
  <div class="container mt-5">
   
    <div class="d-flex bg-secondary text-white p-2 justify-content-between mb-3">
      <h5>Stores List</h5>
      <div class=" me-2">
      <a href="/stores/add" class="btn btn-primary">Add Student</a>
      </div>
    </div>
    <table class="table  table-bordered">
      <thead>
        <tr>
          <th>S.no</th>
          <th>Store Name</th>
          <th>Status</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {{#each stores}}
        <tr>
          <td>{{inc @index}}</td>
          <td>{{this.storeName}}</td>
          <td>{{this.status}}</td>
          <td>
            <a href="/stores/edit/{{this._id}}" class="btn btn-warning btn-sm">Edit</a>
            <form action="/stores/{{this._id}}?_method=DELETE" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger btn-sm">Delete</button>
            </form>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</body>
</html>




